apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarqube
  namespace: sonarqube
spec:
  parentRefs:
    - name: main-gateway
      namespace: kube-system
  hostnames:
    - "sonarqube.k8s.benrachmiel.org"
  rules:
    - backendRefs:
        - name: oauth2-proxy
          port: 4180
