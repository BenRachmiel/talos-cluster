apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mlflow
  namespace: mlflow
spec:
  parentRefs:
    - name: main-gateway
      namespace: kube-system
  hostnames:
    - "mlflow.k8s.benrachmiel.org"
  rules:
    - backendRefs:
        - name: mlflow
          port: 5000
